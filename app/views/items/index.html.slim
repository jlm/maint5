- provide(:title, "Items")
nav.breadcrumbs
  div.card.wide
    span.breadcrumbs__text = @qualifier + " " + committee + " Maintenance Items"
.vspace.flex.justify-between
  .header__title
    h1 = @qualifier + " " + committee + " Maintenance Items"
  .header__actions
    = link_to "New Item", new_item_path, class: 'btn btn-primary', data: { turbo_frame: 'remote_modal' }

table#items.table
  thead: tr
    th = sortable("number")
    th Status
    th = sortable("date", "Submitted")
    th = sortable("standard")
    th = sortable("clause")
    th Subject
    th = sortable("draft", "Draft with fix")
  tbody
    = render @items
