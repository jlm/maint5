doctype html
html[lang="en"]
  head
    = render 'head'
  body
    = render 'layouts/header'
    main
      .flash-container
        = flash_messages
      .container.mt-4
        /! This application.html is based on r8-bs-remote-modals-example with content from maint.
        = yield
      - #= render('layouts/footer')
    = turbo_frame_tag "remote_modal", target: "_top"
